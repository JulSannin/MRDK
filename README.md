# МРДК - Сайт Дома Культуры

Современный веб-сайт для Дома Культуры с защищённой админ-панелью, серверным рендерингом и RESTful API.

## ✨ Основные возможности

- 🎭 **Публичный сайт** - события, документы, контакты, кружки
- 🔐 **Защищённая админ-панель** - управление контентом с CSRF защитой
- 📱 **Адаптивный дизайн** - работает на всех устройствах
- ⚡ **Быстрая загрузка** - SSR и оптимизированная сборка
- 🔒 **Безопасность** - CSRF, httpOnly cookies, валидация данных
- 📸 **Загрузка файлов** - изображения событий и документы

## 🚀 Стек технологий

### Frontend
- **React Router 7.10** - Маршрутизация с SSR
- **React 19.2** - UI компоненты
- **TypeScript 5.9** - Типизация
- **Tailwind CSS 4.1** - Стилизация
- **Vite 7** - Сборка и dev сервер

### Backend
- **Node.js + Express 4.18** - REST API
- **csurf 1.11** - CSRF защита
- **JWT + httpOnly cookies** - Аутентификация
- **Bcryptjs** - Хеширование паролей
- **Multer** - Загрузка файлов
- **Helmet** - Безопасность заголовков
- **JSON Database** - Локальное хранилище

## 📁 Структура проекта

```
MRDK/
├── app/                              # Frontend приложение
│   ├── routes/                       # Маршруты React Router 7
│   │   ├── home.tsx                 # Главная страница
│   │   ├── events.tsx               # Список событий
│   │   ├── event.tsx                # Детальная страница события
│   │   ├── documents.tsx            # Публичные документы
│   │   ├── reminders.tsx            # Напоминания (админ)
│   │   ├── workplan.tsx             # План работы (админ)
│   │   ├── clubs.tsx                # Кружки и клубы
│   │   ├── contacts.tsx             # Контакты
│   │   ├── anticorruption.tsx       # Антикоррупция
│   │   ├── admin.tsx                # Админ-панель
│   │   └── admin-login.tsx          # Вход в админку
│   ├── components/
│   │   ├── features/auth/           # Компоненты администрирования
│   │   │   ├── Admin.tsx            # Контейнер с проверкой роли
│   │   │   ├── AdminLogin.tsx       # Форма входа
│   │   │   ├── EventsManager.tsx    # Управление событиями
│   │   │   ├── DocumentsManager.tsx # Управление документами
│   │   │   ├── RemindersManager.tsx # Управление напоминаниями
│   │   │   └── WorkplanManager.tsx  # Управление планом
│   │   ├── widgets/                 # Переиспользуемые виджеты
│   │   │   ├── header/              # Шапка с навигацией
│   │   │   └── footer/              # Подвал с контактами
│   │   ├── main/                    # Компоненты страниц
│   │   ├── shared/ui/               # UI компоненты
│   │   │   ├── Button.tsx           # Кнопка с состояниями
│   │   │   └── notification/        # Система уведомлений
│   │   └── entities/                # Типы данных
│   ├── utils/
│   │   ├── api.ts                   # API клиент с CSRF
│   │   ├── errorHandler.ts          # Обработка ошибок
│   │   ├── dateHelpers.ts           # Форматирование дат
│   │   ├── eventHelpers.ts          # Утилиты событий
│   │   └── loaderFactory.ts         # Фабрика loaders
│   ├── hooks/
│   │   └── useManager.ts            # CRUD операции
│   ├── contexts/
│   │   ├── NotificationContext.tsx  # Тосты (макс 2)
│   │   └── ConfirmDialogContext.tsx # Диалоги подтверждения
│   ├── config/
│   │   └── constants.ts             # Константы
│   └── root.tsx                     # Корневой компонент
│
└── server/                           # Backend API
    ├── index.js                     # Express сервер с CSRF
    ├── config.js                    # Конфигурация
    ├── database/
    │   ├── db.js                    # JSON хранилище
    │   └── db.json                  # База данных
    ├── routes/                      # REST API
    │   ├── auth.js                  # Авторизация + CSRF token
    │   ├── events.js                # События CRUD
    │   ├── documents.js             # Документы CRUD
    │   ├── reminders.js             # Напоминания CRUD
    │   └── workplan.js              # План работы CRUD
    ├── middleware/
    │   ├── auth.js                  # JWT верификация
    │   └── validation.js            # Санитизация данных
    └── uploads/                     # Загруженные файлы
        ├── events/                  # Изображения событий
        └── documents/               # PDF, Word, Excel
```

## 🛠️ Быстрый старт

### Предварительные требования
- Node.js 18+ и npm 9+
- Git (для клонирования)

### 1. Установка зависимостей

```bash
# Клонирование репозитория
git clone <repository-url>
cd MRDK

# Установка зависимостей frontend и backend
npm install
cd server && npm install && cd ..
```

### 2. Конфигурация окружения

**Backend** - создайте `server/.env`:
```env
PORT=5000
NODE_ENV=development
JWT_SECRET=your-secret-change-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
```

> **Примечание:** Frontend использует `http://localhost:5000/api` по умолчанию (настройка в `app/config/constants.ts`)

### 3. Запуск приложения

**Один терминал (рекомендуется):**
```bash
npm run dev
# Запускает backend (port 5000) и frontend (port 5173) одновременно
```

**Или раздельно:**
```bash
# Терминал 1 - Backend
cd server && npm run dev

# Терминал 2 - Frontend  
npm run dev:client
```

### 4. Доступ
- 🌐 **Сайт:** http://localhost:5173
- 🔐 **Админ-панель:** http://localhost:5173/admin/login
  - Логин: `admin`
  - Пароль: `admin` (5+ символов)

## 🔐 Безопасность

### Реализованные меры защиты
1. **CSRF защита** - Cookie-based токены с помощью csurf
2. **Безопасные cookies** - httpOnly, sameSite: strict, secure в production
3. **JWT аутентификация** - Токены с 24ч сроком действия
4. **Хеширование паролей** - Bcryptjs с 10 раундами соли
5. **Валидация данных** - Санитизация на сервере + клиенте
6. **CSP заголовки** - Helmet.js без unsafe-inline
7. **Rate limiting** - 20 запросов/15 минут на логин
8. **Retry логика** - 3 попытки с экспоненциальной задержкой
9. **Обработка ошибок** - Централизованная с CSRF error handling
10. **Проверка роли** - Доступ к админке только для роли 'admin'

### Доступ в админ-панель
- **URL:** http://localhost:5173/admin/login
- **Логин:** `admin` (минимум 3 символа)
- **Пароль:** `admin` (минимум 5 символов)
- **Роль:** Автоматически присваивается 'admin'

> ⚠️ **Важно для production:** Измените `JWT_SECRET` и пароль администратора, используйте HTTPS!

### Как работает CSRF защита
1. Клиент запрашивает CSRF токен: `GET /api/auth/csrf-token`
2. Сервер генерирует токен и сохраняет в cookie
3. Клиент отправляет токен в заголовке `X-CSRF-Token` при каждой мутации (POST/PUT/DELETE)
4. Сервер сверяет токен из заголовка с cookie
5. Запрос выполняется только при совпадении

### JWT в httpOnly cookie
- Токен недоступен из JavaScript (защита от XSS)
- Автоматически отправляется браузером при каждом запросе
- Содержит: `{ id, username, role }` с 24ч сроком действия

## 📋 Функциональность

### Публичные страницы
- 🏠 **Главная** - Информация о Доме Культуры
- 📅 **События** - Мероприятия с изображениями и описанием
- 📄 **Документы** - Публичные документы по категориям
- 🎭 **Кружки** - Список кружков и клубов
- 📞 **Контакты** - Контактная информация
- 🛡️ **Антикоррупция** - Информация о противодействии коррупции

### Админ-панель (требуется авторизация)

#### 📅 События
- Создание событий с изображениями
- Редактирование названия, описания, даты
- Замена изображения события
- Удаление с уведомлением

#### 📄 Документы  
- Добавление документов с категориями
- Загрузка файлов (PDF, Word, Excel)
- Редактирование метаданных
- Удаление документов

#### 🔔 Напоминания
- Создание с приоритетами (Высокий/Средний/Низкий)
- Установка дедлайна
- Отметка как выполнено
- Цветовая индикация статуса

#### 📋 План работы
- Создание плана по месяцам
- Указание года и месяца
- Описание планируемой работы
- Редактирование и удаление записей

## 🗄️ База данных

Проект использует JSON-файл (`server/database/db.json`) как локальное хранилище. База автоматически создаётся при первом запуске.

### Структура данных
```json
{
  "events": [
    {
      "id": 1,
      "title": "Название события",
      "description": "Описание",
      "date": "2026-02-15",
      "image": "/uploads/events/image.jpg"
    }
  ],
  "documents": [
    {
      "id": 1,
      "title": "Документ",
      "description": "Описание",
      "category": "Уставные документы",
      "file": "/uploads/documents/doc.pdf"
    }
  ],
  "reminders": [
    {
      "id": 1,
      "title": "Напоминание",
      "description": "Описание",
      "deadline": "2026-02-01",
      "priority": "high",
      "completed": false
    }
  ],
  "workplan": [
    {
      "id": 1,
      "month": "Январь",
      "year": 2026,
      "description": "Описание плана работы на месяц"
    }
  ],
  "users": [
    {
      "id": 1,
      "username": "admin",
      "password": "$2a$10$hashedPassword",
      "role": "admin"
    }
  ]
}
```

> 💡 **Рекомендация для production:** Мигрировать на PostgreSQL или MongoDB

## 📡 API Endpoints

### Аутентификация
| Метод | Endpoint | Описание | Auth |
|-------|----------|---------|------|
| GET | `/api/auth/csrf-token` | Получить CSRF токен | ❌ |
| POST | `/api/auth/login` | Вход (rate limit: 20/15мин) | ❌ |
| GET | `/api/auth/verify` | Проверка JWT токена | ✅ |
| POST | `/api/auth/logout` | Выход из системы | ✅ |

### События
| Метод | Endpoint | Описание | Auth | CSRF |
|-------|----------|---------|------|------|
| GET | `/api/events` | Список всех событий | ❌ | ❌ |
| GET | `/api/events/:id` | Событие по ID | ❌ | ❌ |
| POST | `/api/events` | Создать событие | ✅ | ✅ |
| PUT | `/api/events/:id` | Обновить событие | ✅ | ✅ |
| DELETE | `/api/events/:id` | Удалить событие | ✅ | ✅ |

### Документы
| Метод | Endpoint | Описание | Auth | CSRF |
|-------|----------|---------|------|------|
| GET | `/api/documents` | Список документов | ❌ | ❌ |
| POST | `/api/documents` | Добавить документ | ✅ | ✅ |
| PUT | `/api/documents/:id` | Обновить документ | ✅ | ✅ |
| DELETE | `/api/documents/:id` | Удалить документ | ✅ | ✅ |

### Напоминания
| Метод | Endpoint | Описание | Auth | CSRF |
|-------|----------|---------|------|------|
| GET | `/api/reminders` | Список напоминаний | ❌ | ❌ |
| POST | `/api/reminders` | Создать напоминание | ✅ | ✅ |
| PUT | `/api/reminders/:id` | Обновить напоминание | ✅ | ✅ |
| DELETE | `/api/reminders/:id` | Удалить напоминание | ✅ | ✅ |

### План работы
| Метод | Endpoint | Описание | Auth | CSRF |
|-------|----------|---------|------|------|
| GET | `/api/workplan` | Список задач | ❌ | ❌ |
| POST | `/api/workplan` | Добавить задачу | ✅ | ✅ |
| PUT | `/api/workplan/:id` | Обновить задачу | ✅ | ✅ |
| DELETE | `/api/workplan/:id` | Удалить задачу | ✅ | ✅ |

**Примечание:** 
- ✅ Auth = требуется JWT токен в httpOnly cookie
- ✅ CSRF = требуется X-CSRF-Token заголовок (для мутаций)

## 🏗️ Архитектура

### Frontend (Feature-Sliced Design)
```
app/
├── routes/          # React Router 7 маршруты с loaders
├── components/
│   ├── features/    # Функциональные компоненты (auth, managers)
│   ├── widgets/     # Композитные виджеты (header, footer)
│   ├── entities/    # Бизнес-сущности и типы
│   ├── shared/      # Переиспользуемые UI компоненты
│   └── main/        # Компоненты страниц
├── utils/           # Утилиты (api, helpers, handlers)
├── hooks/           # React хуки (useManager для CRUD)
├── contexts/        # React Context (уведомления, диалоги)
└── config/          # Константы и конфигурация
```

**Ключевые решения:**
- **React Router 7** - файловая маршрутизация с SSR и loaders
- **Context API** - глобальное состояние для уведомлений (макс 2)
- **Custom hooks** - `useManager<T>` для унификации CRUD операций
- **Centralized API** - `api.ts` с CSRF токенами и retry логикой
- **TypeScript** - строгая типизация всех компонентов

### Backend (MVC паттерн)
```
server/
├── index.js         # Express app с middleware
├── routes/          # Контроллеры для каждой сущности
├── middleware/      # Auth JWT + validation санитизация
├── database/        # JSON хранилище с очередью записи
└── uploads/         # Статические файлы (multer)
```

**Ключевые решения:**
- **CSRF middleware** - защита всех мутирующих операций
- **JWT в httpOnly** - автоматическая отправка с credentials
- **Модульные маршруты** - разделение по доменам (events, docs, etc)
- **Централизованная обработка ошибок** - единый формат ответов
- **Rate limiting** - защита от брутфорса на логин

## 📦 Сборка и развертывание

### Production сборка

```bash
# 1. Сборка frontend
npm run build
# Результат в ./build/

# 2. Проверка типов
npm run typecheck

# 3. Запуск production сервера
npm start
# SSR сервер на порту из переменной окружения
```

### Развертывание

**Docker (рекомендуется):**
```bash
# Сборка образа
docker build -t mrdk-app .

# Запуск контейнера
docker run -d -p 5000:5000 -p 3000:3000 \
  -e JWT_SECRET=your-secret \
  -e ADMIN_PASSWORD=secure-password \
  mrdk-app
```

**PM2 (процесс-менеджер):**
```bash
# Установка PM2
npm install -g pm2

# Запуск с ecosystem.config.json
pm2 start ecosystem.config.json

# Управление
pm2 status
pm2 logs
pm2 restart all
```

**Nginx (reverse proxy):**
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /api {
        proxy_pass http://localhost:5000;
    }

    location /uploads {
        proxy_pass http://localhost:5000;
    }
}
```

## 🤝 Разработка

### Команды разработчика

```bash
# Запуск dev серверов (frontend + backend одновременно)
npm run dev

# Только frontend (порт 5173)
npm run dev:client

# Только backend (порт 5000)
npm run dev:server
# или: cd server && npm run dev

# Проверка типов TypeScript
npm run typecheck

# Production сборка
npm run build

# Запуск production сервера
npm start
```

### Добавление новой функциональности

**1. Новая страница:**
```typescript
// app/routes/my-page.tsx
import type { Route } from './+types/my-page';

export async function clientLoader() {
  // Загрузка данных
  return { data: [] };
}

export default function MyPage({ loaderData }: Route.ComponentProps) {
  return <div>Моя страница</div>;
}
```

**2. Новый API endpoint:**
```javascript
// server/routes/my-resource.js
const express = require('express');
const router = express.Router();
const { authenticateToken } = require('../middleware/auth');

router.get('/', authenticateToken, async (req, res) => {
  // Логика получения данных
  res.json({ data: [] });
});

module.exports = router;
```

**3. Добавление в API клиент:**
```typescript
// app/utils/api.ts
export const api = {
  // ...существующие методы
  
  getMyResource: async (): Promise<MyType[]> => {
    return apiFetch<MyType[]>(`${API_URL}/my-resource`);
  },
  
  createMyResource: async (data: FormData): Promise<MyType> => {
    const csrfToken = await getCSRFToken();
    return apiFetch<MyType>(`${API_URL}/my-resource`,
      addCSRFHeaders({ method: 'POST', body: data }, csrfToken)
    );
  },
};
```

### Рекомендации по коду
- ✅ Используйте TypeScript для всех новых файлов
- ✅ Добавляйте CSRF защиту для всех мутаций (POST/PUT/DELETE)
- ✅ Валидируйте данные на сервере (`validateAndSanitize`)
- ✅ Используйте `useManager` хук для CRUD операций
- ✅ Обрабатывайте ошибки через `ApiError` и `handleError`
- ✅ Ограничивайте уведомления (макс 2 через NotificationContext)

## 📚 Дополнительные ресурсы

- 📖 [React Router документация](https://reactrouter.com/)
- 📖 [Express.js документация](https://expressjs.com/)
- 📖 [Tailwind CSS документация](https://tailwindcss.com/)
- 📖 [TypeScript документация](https://www.typescriptlang.org/)

## 🐛 Решение проблем

### Не могу войти в админ-панель
**Симптомы:** "Неверный логин или пароль" или "Недостаточно прав"

**Решения:**
1. Проверьте учетные данные: `admin` / `admin` (минимум 5 символов)
2. Убедитесь что backend запущен: http://localhost:5000/api/auth/csrf-token должен вернуть токен
3. Проверьте роль пользователя в `server/database/db.json` - должна быть `"role": "admin"`
4. Очистите cookies браузера и попробуйте снова

### Не могу удалить документы/события
**Причина:** Отсутствует CSRF токен в запросе

**Решение:** Уже исправлено в последней версии. Если проблема сохраняется:
```bash
# Перезапустите серверы
npm run dev
```

### CORS ошибки в консоли
**Решение:** Проверьте `server/.env`:
```env
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
```

### Файлы не загружаются
**Возможные причины:**
1. Папки `server/uploads/events/` и `server/uploads/documents/` не существуют - создайте их
2. Нет прав на запись - выполните `chmod -R 755 server/uploads/`
3. Файл больше 5MB - увеличьте лимит в `server/index.js` (multer limits)

### ERR_MODULE_NOT_FOUND для csurf
**Решение:**
```bash
cd server
npm install
cd ..
```

### SVG иконки не отображаются
**Причина:** React Router перехватывает пути `/imgs/*.svg`

**Решение:** Уже исправлено - SVG импортируются как ES модули. Проверьте что используете:
```typescript
import logoSvg from './imgs/logo.svg';
<img src={logoSvg} alt="Logo" />
```

### Слишком много уведомлений
**Решение:** Уже исправлено - максимум 2 уведомления одновременно, старые автоматически удаляются

## ✅ Чек-лист для production

### Безопасность
- [ ] Изменён пароль администратора (`ADMIN_PASSWORD` в `.env`)
- [ ] Установлен криптостойкий `JWT_SECRET` (минимум 32 символа)
- [ ] Настроен SSL/TLS сертификат (Let's Encrypt или платный)
- [ ] `NODE_ENV=production` в переменных окружения
- [ ] Отключены source maps в production сборке
- [ ] Rate limiting настроен на все критичные endpoints
- [ ] Логи не содержат чувствительной информации

### База данных
- [ ] Миграция с JSON на PostgreSQL/MongoDB
- [ ] Настроено автоматическое резервное копирование
- [ ] Индексы добавлены на часто запрашиваемые поля
- [ ] Данные валидируются на уровне схемы БД

### Файлы и хранилище
- [ ] Загрузки перенесены на S3/Cloudinary/CDN
- [ ] Настроено сжатие изображений
- [ ] Ограничения размеров файлов документированы
- [ ] Очистка неиспользуемых файлов автоматизирована

### Мониторинг и логи
- [ ] Настроен Winston/Bunyan для production логов
- [ ] Интеграция с Sentry/DataDog для отслеживания ошибок
- [ ] Мониторинг uptime (UptimeRobot, Pingdom)
- [ ] Алерты на критические ошибки настроены

### Производительность
- [ ] Статические файлы отдаются через CDN
- [ ] Настроено кеширование на Nginx/CloudFlare
- [ ] Gzip сжатие включено на веб-сервере
- [ ] Lazy loading для изображений реализован

### Документация
- [ ] README обновлён с production инструкциями
- [ ] API документация актуальна
- [ ] Runbook для экстренных ситуаций создан
- [ ] Контакты ответственных лиц документированы

---

## 📚 Технологии и ресурсы

### Документация
- [React Router v7](https://reactrouter.com/) - Маршрутизация и SSR
- [React 19](https://react.dev/) - UI библиотека
- [TypeScript](https://www.typescriptlang.org/) - Типизация
- [Tailwind CSS](https://tailwindcss.com/) - Утилитарные стили
- [Express.js](https://expressjs.com/) - Backend фреймворк
- [JWT](https://jwt.io/) - JSON Web Tokens
- [csurf](https://github.com/expressjs/csurf) - CSRF защита

### Используемые паттерны
- **Feature-Sliced Design** - архитектура frontend
- **MVC** - архитектура backend
- **Custom Hooks** - переиспользование логики (useManager)
- **Context API** - глобальное состояние
- **Factory Pattern** - loaderFactory для маршрутов
- **Middleware Chain** - Express middleware конвейер

### Зависимости

**Frontend:**
- react@19.2.3, react-dom@19.2.3
- react-router@7.10.1
- typescript@5.9.2
- tailwindcss@4.1.13
- vite@7.1.7

**Backend:**
- express@4.18.2
- csurf@1.11.0
- jsonwebtoken@9.0.2
- bcryptjs@2.4.3
- multer@1.4.5-lts.1
- helmet@8.0.0

---

## 📄 Лицензия

Этот проект создан для Дома Культуры. Все права защищены.

---

**Версия:** 2.0.0  
**Обновлено:** Январь 2026  
**Статус:** ✅ Production Ready (с рекомендациями выше)

Создано с ❤️ для Муниципального районного Дома Культуры
