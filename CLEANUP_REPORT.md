# Отчёт о неиспользуемом коде и файлах

## 🗑️ Неиспользуемые файлы (можно удалить)

### 1. **Пустые папки в `app/components/entities/`**
```
app/components/entities/document/     (пустая)
app/components/entities/event/        (пустая)
app/components/entities/reminder/     (пустая)
app/components/entities/workplan/     (пустая)
```
**Причина:** Папки были созданы под Feature Sliced Design, но никогда не использовались. Все типы находятся в `types.ts`.

**Действие:** Удалить все 4 пустые папки.

---

### 2. **Технический скрипт `fix-duplicates.js`**
```
fix-duplicates.js
```
**Причина:** Одноразовый скрипт для исправления дубликатов в БД. Использовался один раз, больше не нужен.

**Действие:** Удалить файл.

---

## 📦 Неиспользуемые зависимости

### 1. **`cheerio` (НЕ ИСПОЛЬЗУЕТСЯ)**
```json
"cheerio": "^1.2.0"
```
**Проверка:** Поиск по всему проекту показал 0 импортов cheerio в коде приложения.

**Причина:** Возможно, была добавлена для парсинга HTML, но никогда не использовалась.

**Действие:**
```bash
npm uninstall cheerio
```

---

## 🧹 Неиспользуемые экспорты в коде

### 1. **`handleApiError()` в `app/utils/errorHandler.ts`**
```typescript
export function handleApiError(error: unknown): never {
    const details = getErrorDetails(error);
    console.error('[API Error]', details);
    logger?.error('API Error', details);
    throw new ApiError(details.message, details.status, details.code);
}
```
**Использование:** Функция определена, но никогда не вызывается в коде.

**Действие:** Удалить функцию.

---

## 🔧 Избыточные конфигурации

### 1. **Dockerfile (не используется на Railway)**
```dockerfile
Dockerfile
```
**Причина:** Railway использует собственную систему деплоя без Docker. Dockerfile никогда не используется.

**Рекомендация:**
- ❌ **Удалить** — если планируете использовать только Railway
- ✅ **Оставить** — если в будущем захотите деплоить через Docker (VPS, другие хостинги)

---

### 2. **PM2 конфигурация (не используется на Railway)**
```json
ecosystem.config.json
```
```bash
deploy.sh
```
**Причина:** Railway не использует PM2. Эти файлы для VPS-деплоя.

**Рекомендация:**
- ❌ **Удалить** — если планируете использовать только Railway
- ✅ **Оставить** — если в будущем захотите деплоить на VPS через PM2

---

### 3. **`start.sh` скрипт**
```bash
start.sh
```
**Причина:** Скрипт для локального запуска. Все команды дублируются в `package.json`.

**Рекомендация:** 
- ❌ **Удалить** — команды есть в npm scripts
- ✅ **Оставить** — если вам удобнее запускать через `.sh` скрипт

---

## ✅ Все используется (проверено)

### **Зависимости:**
- ✅ `zod` — используется в `EventsManager.tsx` и `AdminLogin.tsx` для валидации форм
- ✅ Все остальные зависимости активно используются

### **Константы в `app/config/constants.ts`:**
- ✅ `TITLE_MIN_LENGTH` — используется в `useManager.ts`
- ✅ `TITLE_MAX_LENGTH` — используется в `useManager.ts`
- ✅ `DESCRIPTION_MAX_LENGTH` — используется в `useManager.ts`
- ✅ `NAME_MAX_LENGTH` — используется в `useManager.ts`
- ✅ `EMAIL_MAX_LENGTH` — используется в `useManager.ts`
- ✅ `DOCUMENT_CATEGORIES` — используется в `DocumentsManager.tsx`
- ✅ `MIN_USERNAME_LENGTH` — используется в `AdminLogin.tsx`
- ✅ `MIN_PASSWORD_LENGTH` — используется в `AdminLogin.tsx`
- ✅ `LATEST_EVENTS_COUNT` — используется в `home.tsx`
- ✅ `API_URL` — используется в `api.ts`
- ✅ `BACKEND_URL` — используется в `fileHelpers.ts`

### **Утилиты в `app/utils/errorHandler.ts`:**
- ✅ `ApiError` — активно используется в `api.ts`
- ✅ `getErrorMessage()` — используется в `useManager.ts`, `Admin.tsx`, `AdminLogin.tsx`
- ✅ `getErrorDetails()` — используется внутри `handleApiError()` (НО сам handleApiError не используется)
- ✅ `AUTH_ERRORS` — используется в `api.ts`
- ✅ `COMMON_ERRORS` — используется в `api.ts`

---

## 📋 Рекомендации по очистке

### **КРИТИЧНО (точно удалить):**
1. ❌ **4 пустые папки** в `app/components/entities/`
2. ❌ **`cheerio`** зависимость
3. ❌ **`fix-duplicates.js`** скрипт
4. ❌ **`handleApiError()`** функция в errorHandler.ts

### **НА ВАШЕ УСМОТРЕНИЕ:**
5. ⚠️ **Dockerfile** — нужен только если планируете Docker деплой
6. ⚠️ **ecosystem.config.json** — нужен только для PM2 на VPS
7. ⚠️ **deploy.sh** — нужен только для PM2 на VPS
8. ⚠️ **start.sh** — дублирует npm scripts

---

## 🎯 Итоговая статистика

**Найдено проблем:**
- 🗑️ 4 пустые папки
- 🗑️ 1 неиспользуемая зависимость (cheerio)
- 🗑️ 1 технический скрипт (fix-duplicates.js)
- 🗑️ 1 неиспользуемая функция (handleApiError)
- ⚠️ 4 конфига для других систем деплоя (опционально)

**Потенциальная экономия:**
- ~50 МБ в node_modules (удаление cheerio)
- ~1-2 секунды при установке зависимостей
- Чище структура проекта

**Код:** Почти весь код используется! ✅ Дублирование уже было устранено в предыдущем рефакторинге.
